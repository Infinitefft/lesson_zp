# css 大海

- 何为css？
    - 一个属性与值的键值对为声明
    - 声明块 多个声明组成一个声明块
    - 声明块如果需要作用到对应的HTML元素，选择器
    - cssRules 组成了 样式表

- 层叠？

- margin 重叠 最大的
- 小数单位px 怎么处理的？
- inline 元素在有些情况下不支持transform
    position absolute (不是一个简单的元素了)
    inline -> inline-block

- css 选择器 优先级按 各十百千
从小到大安排 element -> class -> id -> inline
!impotant 最高 不要乱用