# LocalStorage

- 本地
    browser
    会给我们一个空间，用于存储一些配置，永久存储（不会过期）
    key => value（存字符串） JSON.stringify() 

- css 具有继承特性 inherit 
    - font-size color 等 如果没有继承特性，那么手都要敲断
    - background，width，height 等不会继承

- outline overflow
    outline 是轮廓线，有点像 border 但不占据盒模型
    overflow 管住子元素超出父元素的显示

- flex
    - 新的格式化上下文