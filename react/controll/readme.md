# 受控组件和非受控组件
- 怎么拿到表单的值？
  - 表单元素
    value 双向的 需要value，又能输入改变value
  - 状态 单项数据绑定，控制表单元素的值
    input value = {value}
      数据状态驱动页面，状态不能出问题的 受控组件
      - 收集用户的输入，onChange 改变value状态
    - useRef + ref
      .current.value 可以拿到表单元素的值

## 区别
- 非受控组件适合一次性读取，性能敏感，比如文件上传等
- 受控组件适合表单校验，联动、实时展示，比如表单的即时验证