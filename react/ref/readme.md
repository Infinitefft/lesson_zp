# useRef hooks 函数

- useEffect []
  vue onMounted 生命周期
- 怎么找到input 元素
  ref + useRef 存储的是一个可变对象

- 和useState 比较
  - 相同点 都是存储可变对象的容器
    useState 可变带来状态
    useRef 可变对象的存储
  - useState 是响应式的，useRef 不是响应式的

## useRef 应用场景
- dom 节点的引用
- 可变对象的存储来 
- 可创建的是可持久化的引用对象